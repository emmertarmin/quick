# Project Notes

This is a Bun monorepo.

After bigger chunks of work, validate at least with:

```sh
bun run tsc
bun x oxlint
```

Do not read `.env`; use `.env.example` to learn variable names, trust both are up to date.